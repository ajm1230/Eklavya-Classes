import { someNodeModule } from 'some-node-module';

// Example test function with callback
function exampleTest(done: (err?: Error) => void) {
    // Test logic here
    done();
}

// ... (rest of the file content)